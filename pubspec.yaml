name: raw_api_server
description: A Dart package for building small and simple socket-based APIs.
version: 1.1.1
repository: https://www.github.com/tytydraco/raw_api_server

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  collection: ^1.16.0
  path: ^1.8.0

dev_dependencies:
  test: ^1.16.0
  very_good_analysis: ^3.0.1
